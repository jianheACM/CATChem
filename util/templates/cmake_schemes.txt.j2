# {{ process_config.name }} Schemes CMakeLists.txt
# Generated by CATChem Process Generator

{% for scheme in process_config.schemes %}
# Add {{ scheme.name }} scheme
add_library({{ scheme.name|lower }}_scheme
    {{ scheme.name }}Scheme_Mod.F90
)

# Link dependencies
target_link_libraries({{ scheme.name|lower }}_scheme
    precision_mod
    constants
)

# Include directories
target_include_directories({{ scheme.name|lower }}_scheme PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
)

# Install targets
install(TARGETS {{ scheme.name|lower }}_scheme
    DESTINATION lib
)

# Install module files
install(FILES
    ${CMAKE_CURRENT_BINARY_DIR}/{{ scheme.name|lower }}_mod.mod
    DESTINATION include
)

{% endfor %}
